{
    "version": "0.2.0",
    "configurations": [
        {
          "name": "C++ Launch",
          "type": "codelldb",
          "request": "launch",
          "program": "${workspaceFolder}/build/test",
          "environment": [{ "name": "config", "value": "Debug" }],
          "cwd": "${workspaceFolder}"
        }
    ]
}
